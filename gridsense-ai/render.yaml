services:
  # ──────────────────────────────────────────
  # Backend — FastAPI (Docker)
  # ──────────────────────────────────────────
  - type: web
    name: gridsense-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./gridsense.db
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: LOG_LEVEL
        value: INFO
      - key: SYNTHETIC_DATA_BANNER
        value: "true"
      - key: LLM_ENABLED
        value: "true"
      - key: LLM_MOCK_MODE
        value: "false"
      - key: LLM_MAX_RETRIES
        value: "3"
      - key: LLM_TIMEOUT_SECONDS
        value: "15"
      - key: GEMINI_PRIMARY_MODEL
        value: gemini-2.0-flash
      - key: GEMINI_REPORT_MODEL
        value: gemini-1.5-pro
      - key: GEMINI_API_KEY
        sync: false

  # ──────────────────────────────────────────
  # Frontend — React/Vite (Static Site)
  # ──────────────────────────────────────────
  - type: static_site
    name: gridsense-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    # No 'plan: free' needed for static sites on Render (they are always free)
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          name: gridsense-backend
          type: web
          property: host
